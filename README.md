# 📝 CLI To-Do Application - Phase 1 (Complete)

A feature-rich Command Line Interface (CLI) application built with Python to manage your daily tasks efficiently.

## 🚀 Features
This project covers all three levels of Phase 1:

### 1. Basic Level
* **Add Tasks**: Create new tasks with ease.
* **View List**: Display all tasks in a structured format.
* **Complete/Delete**: Mark tasks as done or remove them from the list.

### 2. Intermediate Level
* **Priority Support**: Assign **High, Medium, or Low** priority to tasks.
* **Search Functionality**: Quickly find tasks using keywords.
* **Data Persistence**: All tasks are saved in a `tasks.json` file, so your data is never lost.

### 3. Advanced Level (Highlights)
* **Rich UI**: Interactive and beautiful dashboard using the **Rich** library (Tables, Panels, and Colors).
* **Task Filtering**: Filter your list based on Priority.
* **Recurring Tasks**: Support for **Daily** and **Weekly** tasks.
* **Overdue Alerts**: Visual indicators for tasks that are past their due date.

## 🛠️ Tech Stack
* **Language**: Python 3.x
* **Libraries**: Rich (for UI), Argparse (for CLI), JSON (for storage)

## 📦 How to Run
1. Clone the repository.
2. Install the required library:
   ```bash
   pip install rich